/**
 * Package containing all the controllers used in the application.
 *
 * Each controller is bound to an FXML file, which represents it.
 * Each controller must also extend {@link nl.tudelft.context.controller.AbstractController}.
 *
 * @version 1.0
 * @since 13-5-2015
 */
package nl.tudelft.context.controller;
